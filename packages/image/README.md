# @rebimboca/image

Image utilities.

## Tools

### Generators

#### `generateImage(options: { width: number; height: number; text?: string; background?: string; color?: string }): string`

Retorna URL, SVG string ou Data URL com a imagem gerada.

## Installation

```bash
pnpm add @rebimboca/image
```
