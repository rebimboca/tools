---
"@rebimboca/seo": minor
---

Adiciona suporte a geração de metadados para otimização de busca:

- Geração dinâmica de Sitemaps XML válidos (`generateSitemap`).
- Geração automatizada de arquivos `robots.txt` a partir de configurações estruturadas (`generateRobotsTxt`).
